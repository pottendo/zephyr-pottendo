# Copyright (c) 2023 pottendo
# SPDX-License-Identifier: Apache-2.0

############################################## RVCop64 original
if BOARD_ORANGECART

config BOARD
	default "orangecart"

config ARCH_HAS_GDBSTUB
    default n

config SPI
	default y

config FPU
    bool "Support floating point operations"
	default n

config DISK_DRIVER_SDMMC
        default y if DISK_DRIVERS

config C64_MEMBASE
	hex "memory address, where bitstream maps the C64 64k"
	default 0x00000000

endif # BOARD_ORANGECART

############################################## FPU
if BOARD_ORANGECART_FPU
config BOARD
	default "orangecart_fpu"

config SPI
	default y

config FPU
    bool "Support floating point operations"
	default y

config DISK_DRIVER_SDMMC
        default y if DISK_DRIVERS

config C64_MEMBASE
	hex "memory address, where bitstream maps the C64 64k"
	default 0x0f000000

endif # BOARD_ORANGECART_FPU

############################################## SMP
if BOARD_ORANGECART_SMP
config BOARD
	default "orangecart_smp"

config SPI
	default y

config FPU
    bool "Support floating point operations"
	default n

config DISK_DRIVER_SDMMC
        default y if DISK_DRIVERS

config C64_MEMBASE
	hex "memory address, where bitstream maps the C64 64k"
	default 0x0f000000

endif # BOARD_ORANGECART_SMP
